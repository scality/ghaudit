[build-system]
requires = [
  "setuptools>=42",
  "wheel"
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79

[tool.pylint.'MESSAGES CONTROL']
disable = [
        "consider-using-f-string"
]

[tool.isort]
profile = "black"
line_length = 79
